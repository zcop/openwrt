From 186623f4aa724c46cbb4dbd5235cf6942215f5b5 Mon Sep 17 00:00:00 2001
From: David Yang <mmyangfl@gmail.com>
Date: Fri, 17 Oct 2025 14:08:55 +0800
Subject: [PATCH] net: dsa: yt921x: add Kconfig and Makefile entries

Keep only the Kconfig and Makefile wiring in the 6.12 backport tree.
Driver sources are synced from a Linux tree into target/linux/generic/files.

Signed-off-by: David Yang <mmyangfl@gmail.com>
---
 drivers/net/dsa/Kconfig  | 13 +++++++++++++
 drivers/net/dsa/Makefile | 11 +++++++++++
 2 files changed, 24 insertions(+)

--- a/drivers/net/dsa/Kconfig
+++ b/drivers/net/dsa/Kconfig
@@ -159,4 +159,25 @@ config NET_DSA_VITESSE_VSC73XX_PLATFORM
 	  This enables support for the Vitesse VSC7385, VSC7388, VSC7395
 	  and VSC7398 SparX integrated ethernet switches, connected over
 	  a CPU-attached address bus and work in memory-mapped I/O mode.
+
+config NET_DSA_YT921X
+	tristate "Motorcomm YT9215 ethernet switch chip support"
+	select NET_DSA_TAG_YT921X
+	help
+	  This enables support for the Motorcomm YT9215 ethernet switch
+	  chip.
+
+config NET_DSA_YT921X_DEBUG
+	bool "Motorcomm YT9215 debug command interface"
+	depends on NET_DSA_YT921X
+	help
+	  Enable debug command interface under debugfs for yt921x.
+	  This exposes raw register/table helpers and is intended for
+	  bring-up and reverse engineering.
+
+config NET_DSA_YT921X_CR881X
+	bool "Motorcomm YT921x CR881x conduit isolation policy"
+	depends on NET_DSA_YT921X
+	help
+	  Enable CR881x-specific secondary conduit user-port isolation policy.
 endmenu
--- a/drivers/net/dsa/Makefile
+++ b/drivers/net/dsa/Makefile
@@ -18,7 +18,18 @@ obj-$(CONFIG_NET_DSA_SMSC_LAN9303_MDIO) += lan9303_mdio.o
 obj-$(CONFIG_NET_DSA_VITESSE_VSC73XX) += vitesse-vsc73xx-core.o
 obj-$(CONFIG_NET_DSA_VITESSE_VSC73XX_PLATFORM) += vitesse-vsc73xx-platform.o
 obj-$(CONFIG_NET_DSA_VITESSE_VSC73XX_SPI) += vitesse-vsc73xx-spi.o
+obj-$(CONFIG_NET_DSA_YT921X) += yt921x_driver.o
+yt921x_driver-y := \
+	yt921x.o \
+	yt921x_core.o \
+	yt921x_mbus.o \
+	yt921x_qos_tc.o \
+	yt921x_acl.o \
+	yt921x_l2.o \
+	yt921x_port.o \
+	yt921x_chip.o \
+	yt921x_debugfs.o
 obj-y				+= b53/
 obj-y				+= hirschmann/
 obj-y				+= microchip/
